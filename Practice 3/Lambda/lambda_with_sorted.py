# Тізім ішіндегі tuple-дар (есімі, жасы)
users = [("Асан", 25), ("Үсен", 18), ("Арман", 30)]

# Жасы бойынша сұрыптау (екінші элемент - x[1])
sorted_by_age = sorted(users, key=lambda x: x[1])

print(sorted_by_age) 
# Нәтиже: [('Үсен', 18), ('Асан', 25), ('Арман', 30)]